# React Toast Notification System

Продвинутая система уведомлений для React-приложений с умным таймером, дедупликацией и анимациями.

## ✨ Особенности

- **Умный таймер** — приостанавливается при наведении курсора и продолжает с места остановки
- **Дедупликация** — предотвращает создание дублирующихся уведомлений
- **Плавные анимации** — появление и исчезновение с CSS-транзишенами
- **Типизированный TypeScript** — полная типобезопасность
- **Тестирование** — комплексные тесты бизнес-логики с Vitest
- **Никаких сторонних зависимостей** — чистый React

## 🚀 Быстрый старт

### Установка

```bash
npm install
```

### Запуск

```bash
# Development
npm run dev

# Сборка
npm run build

# Тесты
npm run test
```

## 📖 Использование

### Базовое использование

```tsx
import { ToastProvider } from './context/ToastProvider';
import { useToast } from './context/useToast';


function App() {
  return (
    <ToastProvider>
      <YourComponent />
    </ToastProvider>
  );
}

function YourComponent() {
  const { addToast } = useToast();

  const handleSuccess = () => {
    addToast({
      message: 'Операция выполнена успешно!',
      type: 'success'
    });
  };

  return <button onClick={handleSuccess}>Показать уведомление</button>;
}
```

### API

#### `addToast(options: ToastOptions)`

Создает новое уведомление или обновляет существующее.

```tsx
interface ToastOptions {
  message: string;
  type: 'success' | 'error' | 'warning';
  duration?: number; // по умолчанию 3000ms
}
```

#### Типы уведомлений

- `success` — зеленое уведомление об успехе
- `error` — красное уведомление об ошибке  
- `warning` — желтое предупреждение

## 🧠 Бизнес-логика

### 1. Умный таймер (Smart Timer)

Уведомление исчезает через заданное время (`duration`). При наведении курсора таймер **приостанавливается** и **продолжается с момента остановки** после ухода курсора.

**Пример:** если прошло 2 секунды из 3-х, после паузы останется 1 секунда.

### 2. Анти-спам и дедупликация

При попытке создать тост с таким же текстом и типом, который уже отображается, **новый тост не создается**. Вместо этого таймер существующего тоста **сбрасывается в начало**.

### 3. Анимации

- **Появление** — плавное проявление при монтировании
- **Исчезновение** — плавное затухание перед удалением

## 🧪 Тестирование

Проект включает комплексные тесты бизнес-логики:

```bash
npm run test
```

### Покрытие тестами

- ✅ Базовый рендеринг
- ✅ Умный таймер (pause/resume)
- ✅ Множественные hover-циклы
- ✅ Дедупликация уведомлений
- ✅ Ручное закрытие
- ✅ Обработка крайних случаев
- ✅ Очистка таймеров при размонтировании

## 🏗️ Архитектура

```
src/
├── components/
│   ├── ToastItem.tsx          # Компонент отдельного тоста
│   └── ToastItem.test.tsx    # Тесты компонента
├── context/
│   ├── ToastContext.ts       # Типы и интерфейсы
│   ├── ToastProvider.tsx     # Provider с логикой дедупликации
│   └── useToast.ts           # Hook для использования тостов
├── hooks/
│   └── useToastAnimation.ts  # Hook управления таймером и анимациями
└── App.tsx                   # Демо-приложение
```

## 🔧 Технологии

- **React 19** — современный React с последними возможностями
- **TypeScript** — строгая типизация
- **Vite** — быстрая сборка и разработка
- **Vitest** — современное тестирование
- **CSS** — стили без сторонних библиотек

## 📝 Лицензия

MIT License

---

**Создано как тестовое задание** с демонстрацией продвинутых техник React-разработки.
